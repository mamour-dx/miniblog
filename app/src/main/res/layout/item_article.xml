<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="4dp"
    card_view:strokeWidth="0dp"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Titre de l’article -->
        <TextView
            android:id="@+id/tvItemTitre"
            style="@style/TextAppearance.BlogApp.Title3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Titre de l’article" />

        <!-- Aperçu du contenu -->
        <TextView
            android:id="@+id/tvItemPreview"
            style="@style/TextAppearance.BlogApp.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Aperçu du contenu de l’article…" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
